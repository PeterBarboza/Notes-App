{
  "version": 2,
  "buildCommand": "yarn run build",
  "installCommand": "yarn install",
  "devCommand": "yarn run dev:DEBUG",
  "outputDirectory": "./dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/index.js"
    }
  ]
}